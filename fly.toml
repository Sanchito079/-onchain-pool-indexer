# fly.toml app configuration file generated for onchain-pool-indexer on 2026-08-21T20:15:30Z
#
# See https://fly.io/docs/reference/configuration/ for information about how to use this file.
#

app = 'onchain-pool-indexer'
primary_region = 'ams'

[build]
  dockerfile = 'Dockerfile'

[env]
  BACKFILL_SKIP = 'clmm'

[processes]
  app = '/app/indexer'

[http_service]
  internal_port = 8080
  force_https = true
  auto_stop_machines = true
  auto_start_machines = true
  min_machines_running = 0
  processes = ['app']

[[vm]]
  memory = '1gb'
  cpu_kind = 'shared'
  cpus = 1
  memory_mb = 256

[[vm]]
  memory = '122gb'
  cpu_kind = 'performance'
  cpus = 16
  memory_mb = 124928
  processes = ['app']
  size = 'performance-cpu-16x'
